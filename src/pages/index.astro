---
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content={Astro.generator} />
    <title>i am ONE with — Mind, Body & Spirit</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,600;1,300;1,400&family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet" />
    <style>
      *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

      :root {
        --navy:    #1a3a5c;
        --teal:    #3d9e96;
        --gold:    #c9a227;
        --crimson: #bf3a6b;
        --white:   #ffffff;
        --cream:   #f7f6f4;
        --muted:   #7a7a7a;
        --border:  #e4e0da;
        --display: 'Cormorant Garamond', Georgia, serif;
        --body:    'Inter', system-ui, sans-serif;
      }

      html { scroll-behavior: smooth; }
      body { background: var(--white); color: #1a1a1a; font-family: var(--body); }
      img  { display: block; max-width: 100%; }
      a    { text-decoration: none; color: inherit; }

      /* ── ANNOUNCEMENT BAR ─────────────────────────────────── */
      .announcement {
        background: var(--navy);
        color: var(--white);
        text-align: center;
        font-size: 0.8rem;
        letter-spacing: 0.08em;
        padding: 0.5rem 1rem;
        font-family: var(--body);
      }
      .announcement a { color: var(--gold); text-decoration: underline; }

      /* ── NAV ──────────────────────────────────────────────── */
      nav {
        position: sticky;
        top: 0;
        z-index: 100;
        background: var(--white);
        border-bottom: 1px solid var(--border);
        padding: 0 1.5rem;
        display: flex;
        align-items: center;
        justify-content: space-between;
        height: 72px;
        gap: 1rem;
      }
      .nav-logo img { height: 56px; width: auto; max-width: 180px; object-fit: contain; }
      .nav-links {
        display: flex;
        gap: 0;
        list-style: none;
        height: 100%;
      }
      .nav-links > li {
        position: relative;
        display: flex;
        align-items: center;
      }
      .nav-links > li > a {
        padding: 0 0.65rem;
        font-size: 0.72rem;
        font-weight: 500;
        letter-spacing: 0.08em;
        text-transform: uppercase;
        color: var(--navy);
        height: 100%;
        display: flex;
        align-items: center;
        transition: color 0.2s;
        white-space: nowrap;
      }
      .nav-links > li > a:hover { color: var(--teal); }
      .nav-links > li > a.has-drop::after {
        content: '▾';
        margin-left: 4px;
        font-size: 0.7rem;
      }
      .dropdown {
        display: none;
        position: absolute;
        top: 100%;
        left: 0;
        background: var(--white);
        border: 1px solid var(--border);
        border-top: 2px solid var(--teal);
        min-width: 180px;
        box-shadow: 0 8px 24px rgba(0,0,0,0.08);
        z-index: 200;
      }
      .nav-links > li:hover .dropdown { display: block; }
      .dropdown a {
        display: block;
        padding: 0.75rem 1.25rem;
        font-size: 0.8rem;
        letter-spacing: 0.05em;
        color: var(--navy);
        border-bottom: 1px solid var(--border);
        transition: background 0.15s;
      }
      .dropdown a:last-child { border-bottom: none; }
      .dropdown a:hover { background: var(--cream); color: var(--teal); }
      .nav-cta {
        background: var(--teal);
        color: var(--white) !important;
        padding: 0.55rem 1.5rem !important;
        border-radius: 2px;
        font-size: 0.78rem !important;
        letter-spacing: 0.1em;
        font-weight: 500 !important;
        transition: background 0.2s !important;
      }
      .nav-cta:hover { background: var(--navy) !important; color: var(--white) !important; }

      /* ── HERO ─────────────────────────────────────────────── */
      .hero {
        position: relative;
        height: 90vh;
        min-height: 580px;
        background: linear-gradient(
          160deg,
          var(--navy) 0%,
          #234d70 40%,
          #2d6e6a 75%,
          var(--teal) 100%
        );
        display: flex;
        align-items: center;
        justify-content: center;
        text-align: center;
        overflow: hidden;
      }
      .hero-pattern {
        position: absolute;
        inset: 0;
        opacity: 0.06;
        background-image: radial-gradient(circle at 2px 2px, white 1px, transparent 0);
        background-size: 40px 40px;
      }
      .hero-content {
        position: relative;
        z-index: 2;
        max-width: 780px;
        padding: 2rem;
      }
      .hero-eyebrow {
        font-family: var(--body);
        font-size: 0.75rem;
        letter-spacing: 0.25em;
        text-transform: uppercase;
        color: var(--gold);
        margin-bottom: 1.5rem;
      }
      .hero h1 {
        font-family: var(--display);
        font-size: clamp(3rem, 7vw, 6rem);
        font-weight: 300;
        color: var(--white);
        line-height: 1.05;
        margin-bottom: 1.5rem;
        letter-spacing: -0.01em;
      }
      .hero h1 em { font-style: italic; color: #a8ddd9; }
      .hero p {
        font-size: 1.1rem;
        color: rgba(255,255,255,0.8);
        line-height: 1.8;
        max-width: 520px;
        margin: 0 auto 2.5rem;
        font-weight: 300;
      }
      .hero-buttons { display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap; }
      .btn {
        display: inline-block;
        padding: 0.85rem 2.25rem;
        font-size: 0.8rem;
        font-weight: 500;
        letter-spacing: 0.12em;
        text-transform: uppercase;
        border-radius: 2px;
        transition: all 0.2s;
      }
      .btn-light {
        background: var(--white);
        color: var(--navy);
      }
      .btn-light:hover { background: var(--gold); color: var(--white); }
      .btn-outline {
        border: 1.5px solid rgba(255,255,255,0.6);
        color: var(--white);
      }
      .btn-outline:hover { background: rgba(255,255,255,0.1); border-color: var(--white); }
      .hero-scroll {
        position: absolute;
        bottom: 2rem;
        left: 50%;
        transform: translateX(-50%);
        color: rgba(255,255,255,0.5);
        font-size: 0.7rem;
        letter-spacing: 0.2em;
        text-transform: uppercase;
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 0.5rem;
      }
      .hero-scroll::after {
        content: '';
        width: 1px;
        height: 40px;
        background: rgba(255,255,255,0.3);
        display: block;
        animation: scrollline 1.6s ease-in-out infinite;
      }
      @keyframes scrollline {
        0%, 100% { opacity: 0; transform: scaleY(0); transform-origin: top; }
        50%       { opacity: 1; transform: scaleY(1); transform-origin: top; }
      }

      /* ── SECTION COMMONS ──────────────────────────────────── */
      .section-header {
        text-align: center;
        margin-bottom: 3.5rem;
      }
      .eyebrow {
        font-size: 0.72rem;
        letter-spacing: 0.2em;
        text-transform: uppercase;
        color: var(--teal);
        font-weight: 600;
        margin-bottom: 0.75rem;
      }
      .section-header h2 {
        font-family: var(--display);
        font-size: clamp(2rem, 4vw, 3.2rem);
        font-weight: 300;
        color: var(--navy);
        line-height: 1.2;
        margin-bottom: 1rem;
      }
      .section-header h2 em { font-style: italic; color: var(--teal); }
      .section-header p {
        color: var(--muted);
        font-size: 1rem;
        line-height: 1.8;
        max-width: 540px;
        margin: 0 auto;
        font-weight: 300;
      }

      /* ── INTRO STRIP ──────────────────────────────────────── */
      .intro-strip {
        background: var(--cream);
        padding: 4rem 2rem;
        text-align: center;
        border-bottom: 1px solid var(--border);
      }
      .intro-strip p {
        font-family: var(--display);
        font-size: clamp(1.3rem, 3vw, 2rem);
        font-weight: 300;
        color: var(--navy);
        line-height: 1.6;
        max-width: 720px;
        margin: 0 auto;
        font-style: italic;
      }
      .intro-strip p span { color: var(--teal); font-style: normal; font-weight: 400; }

      /* ── SERVICES GRID ────────────────────────────────────── */
      .services {
        padding: 6rem 2rem;
        max-width: 1200px;
        margin: 0 auto;
      }
      .services-grid {
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        gap: 1.5rem;
      }
      @media (max-width: 1024px) { .services-grid { grid-template-columns: repeat(2, 1fr); } }
      @media (max-width: 640px)  { .services-grid { grid-template-columns: 1fr; } }

      .service-card {
        background: var(--white);
        border: 1px solid var(--border);
        border-radius: 3px;
        overflow: hidden;
        display: flex;
        flex-direction: column;
        transition: box-shadow 0.25s, transform 0.25s;
      }
      .service-card:hover {
        box-shadow: 0 12px 40px rgba(26,58,92,0.12);
        transform: translateY(-4px);
      }
      .service-img {
        height: 220px;
        width: 100%;
        object-fit: cover;
        background: var(--cream);
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 2.5rem;
      }
      .service-body {
        padding: 1.5rem;
        flex: 1;
        display: flex;
        flex-direction: column;
        gap: 0.75rem;
      }
      .service-tag {
        font-size: 0.68rem;
        letter-spacing: 0.15em;
        text-transform: uppercase;
        color: var(--teal);
        font-weight: 600;
      }
      .service-body h3 {
        font-family: var(--display);
        font-size: 1.35rem;
        font-weight: 400;
        color: var(--navy);
        line-height: 1.3;
      }
      .service-body p {
        font-size: 0.875rem;
        color: var(--muted);
        line-height: 1.75;
        flex: 1;
        font-weight: 300;
      }
      .service-link {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        font-size: 0.78rem;
        font-weight: 600;
        letter-spacing: 0.1em;
        text-transform: uppercase;
        color: var(--teal);
        margin-top: 0.5rem;
        transition: gap 0.2s;
      }
      .service-link:hover { gap: 0.8rem; color: var(--navy); }
      .service-link::after { content: '→'; }

      /* ── SPLIT FEATURE ────────────────────────────────────── */
      .feature-split {
        background: var(--cream);
        display: grid;
        grid-template-columns: 1fr 1fr;
      }
      @media (max-width: 768px) { .feature-split { grid-template-columns: 1fr; } }
      .feature-visual {
        background: linear-gradient(135deg, var(--navy) 0%, var(--teal) 100%);
        min-height: 480px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 5rem;
        position: relative;
        overflow: hidden;
      }
      .feature-visual::before {
        content: '';
        position: absolute;
        width: 300px;
        height: 300px;
        border-radius: 50%;
        border: 1px solid rgba(255,255,255,0.15);
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
      }
      .feature-visual::after {
        content: '';
        position: absolute;
        width: 200px;
        height: 200px;
        border-radius: 50%;
        border: 1px solid rgba(255,255,255,0.1);
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
      }
      .feature-visual-icon { position: relative; z-index: 2; }
      .feature-text {
        padding: 5rem 4rem;
        display: flex;
        flex-direction: column;
        justify-content: center;
        gap: 1.5rem;
      }
      .feature-text .eyebrow { text-align: left; }
      .feature-text h2 {
        font-family: var(--display);
        font-size: clamp(1.8rem, 3vw, 2.8rem);
        font-weight: 300;
        color: var(--navy);
        line-height: 1.25;
      }
      .feature-text h2 em { font-style: italic; color: var(--teal); }
      .feature-text p {
        color: var(--muted);
        line-height: 1.85;
        font-size: 0.95rem;
        font-weight: 300;
        max-width: 420px;
      }
      .feature-list {
        list-style: none;
        display: flex;
        flex-direction: column;
        gap: 0.6rem;
      }
      .feature-list li {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        font-size: 0.9rem;
        color: var(--navy);
      }
      .feature-list li::before {
        content: '';
        width: 20px;
        height: 1px;
        background: var(--teal);
        flex-shrink: 0;
      }

      /* ── EVENTS ───────────────────────────────────────────── */
      .events {
        padding: 6rem 2rem;
        max-width: 1200px;
        margin: 0 auto;
      }
      .events-grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 1.5rem;
      }
      @media (max-width: 900px) { .events-grid { grid-template-columns: 1fr 1fr; } }
      @media (max-width: 580px) { .events-grid { grid-template-columns: 1fr; } }

      .event-card {
        border: 1px solid var(--border);
        border-radius: 3px;
        overflow: hidden;
        display: flex;
        flex-direction: column;
        transition: box-shadow 0.25s;
      }
      .event-card:hover { box-shadow: 0 8px 30px rgba(26,58,92,0.1); }
      .event-date-bar {
        background: var(--navy);
        color: var(--white);
        padding: 0.75rem 1.25rem;
        display: flex;
        align-items: center;
        justify-content: space-between;
        font-size: 0.8rem;
      }
      .event-date-bar .date {
        font-family: var(--display);
        font-size: 1.6rem;
        font-weight: 300;
        line-height: 1;
      }
      .event-date-bar .month-year {
        text-align: right;
        font-size: 0.7rem;
        letter-spacing: 0.1em;
        text-transform: uppercase;
        opacity: 0.7;
      }
      .event-body { padding: 1.5rem; flex: 1; display: flex; flex-direction: column; gap: 0.6rem; }
      .event-type {
        font-size: 0.68rem;
        letter-spacing: 0.15em;
        text-transform: uppercase;
        color: var(--crimson);
        font-weight: 600;
      }
      .event-body h3 {
        font-family: var(--display);
        font-size: 1.2rem;
        font-weight: 400;
        color: var(--navy);
      }
      .event-meta {
        font-size: 0.8rem;
        color: var(--muted);
        display: flex;
        flex-direction: column;
        gap: 0.25rem;
        margin-top: 0.25rem;
      }
      .event-meta span { display: flex; align-items: center; gap: 0.4rem; }
      .event-cta {
        margin-top: auto;
        padding-top: 1rem;
        border-top: 1px solid var(--border);
        margin-left: 1.5rem;
        margin-right: 1.5rem;
        margin-bottom: 1.5rem;
        display: flex;
        justify-content: space-between;
        align-items: center;
        font-size: 0.78rem;
      }
      .event-price { font-weight: 600; color: var(--navy); }
      .event-book {
        background: var(--teal);
        color: var(--white);
        padding: 0.45rem 1rem;
        border-radius: 2px;
        font-size: 0.72rem;
        font-weight: 600;
        letter-spacing: 0.08em;
        text-transform: uppercase;
        transition: background 0.2s;
      }
      .event-book:hover { background: var(--navy); }

      .events-coming-soon {
        text-align: center;
        padding: 4rem 2rem;
        border: 1px dashed var(--border);
        border-radius: 4px;
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 1rem;
      }
      .coming-soon-icon { font-size: 3rem; }
      .events-coming-soon h3 {
        font-family: var(--display);
        font-size: 1.8rem;
        font-weight: 300;
        color: var(--navy);
      }
      .events-coming-soon p { color: var(--muted); max-width: 400px; line-height: 1.7; }

      /* ── TESTIMONIALS ─────────────────────────────────────── */
      .testimonials {
        background: var(--navy);
        padding: 6rem 2rem;
        overflow: hidden;
      }
      .testimonials .section-header h2 { color: var(--white); }
      .testimonials .section-header p  { color: rgba(255,255,255,0.6); }
      .testimonials .eyebrow           { color: var(--gold); }
      .testimonials-track-wrap {
        position: relative;
        max-width: 800px;
        margin: 0 auto;
      }
      .testimonials-track {
        overflow: hidden;
      }
      .testimonial-slide {
        display: none;
        text-align: center;
        padding: 0 1rem;
        animation: fadein 0.5s ease;
      }
      .testimonial-slide.active { display: block; }
      @keyframes fadein { from { opacity: 0; transform: translateY(8px); } to { opacity: 1; transform: translateY(0); } }
      .testimonial-quote {
        font-family: var(--display);
        font-size: clamp(1.3rem, 3vw, 2rem);
        font-weight: 300;
        font-style: italic;
        color: var(--white);
        line-height: 1.55;
        margin-bottom: 2rem;
      }
      .testimonial-quote::before { content: '\201C'; color: var(--gold); }
      .testimonial-quote::after  { content: '\201D'; color: var(--gold); }
      .testimonial-author { color: rgba(255,255,255,0.6); font-size: 0.85rem; letter-spacing: 0.05em; }
      .testimonial-author strong { color: var(--gold); display: block; font-size: 0.9rem; margin-bottom: 0.2rem; }
      .testimonial-dots {
        display: flex;
        justify-content: center;
        gap: 0.5rem;
        margin-top: 2.5rem;
      }
      .dot {
        width: 8px; height: 8px;
        border-radius: 50%;
        background: rgba(255,255,255,0.25);
        cursor: pointer;
        transition: background 0.2s, transform 0.2s;
        border: none;
      }
      .dot.active { background: var(--gold); transform: scale(1.3); }

      /* ── PHILOSOPHY STRIP ─────────────────────────────────── */
      .philosophy {
        background: var(--gold);
        padding: 5rem 2rem;
        text-align: center;
      }
      .philosophy h2 {
        font-family: var(--display);
        font-size: clamp(1.8rem, 4vw, 3rem);
        font-weight: 300;
        color: var(--white);
        font-style: italic;
        line-height: 1.35;
        max-width: 680px;
        margin: 0 auto 2rem;
      }
      .philosophy .btn {
        background: var(--white);
        color: var(--navy);
      }
      .philosophy .btn:hover { background: var(--navy); color: var(--white); }

      /* ── FOOTER ───────────────────────────────────────────── */
      footer {
        background: #0f2438;
        color: rgba(255,255,255,0.6);
        padding: 4rem 2rem 2rem;
      }
      .footer-grid {
        display: grid;
        grid-template-columns: 2fr 1fr 1fr 1fr;
        gap: 3rem;
        max-width: 1200px;
        margin: 0 auto 3rem;
      }
      @media (max-width: 900px) { .footer-grid { grid-template-columns: 1fr 1fr; } }
      @media (max-width: 560px) { .footer-grid { grid-template-columns: 1fr; } }
      .footer-brand img { height: 36px; width: auto; margin-bottom: 1rem; opacity: 0.85; filter: brightness(0) invert(1); }
      .footer-brand p { font-size: 0.85rem; line-height: 1.75; font-weight: 300; max-width: 260px; }
      .footer-col h4 {
        font-size: 0.72rem;
        letter-spacing: 0.15em;
        text-transform: uppercase;
        color: var(--white);
        margin-bottom: 1.25rem;
        font-weight: 600;
      }
      .footer-col ul { list-style: none; display: flex; flex-direction: column; gap: 0.6rem; }
      .footer-col a { font-size: 0.85rem; transition: color 0.2s; }
      .footer-col a:hover { color: var(--teal); }
      .footer-bottom {
        max-width: 1200px;
        margin: 0 auto;
        padding-top: 2rem;
        border-top: 1px solid rgba(255,255,255,0.1);
        display: flex;
        justify-content: space-between;
        align-items: center;
        flex-wrap: wrap;
        gap: 1rem;
        font-size: 0.78rem;
      }
      .social-links { display: flex; gap: 1rem; }
      .social-links a {
        width: 34px; height: 34px;
        border: 1px solid rgba(255,255,255,0.2);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 0.85rem;
        transition: all 0.2s;
      }
      .social-links a:hover { border-color: var(--teal); color: var(--teal); }
    </style>
  </head>
  <body>

    <!-- ANNOUNCEMENT BAR -->
    <div class="announcement">
      Retreat 2 Connect — Coming Soon
    </div>

    <!-- NAV -->
    <nav>
      <div class="nav-logo">
        <a href="/"><img src="/logo.png" alt="i am ONE with" /></a>
      </div>
      <ul class="nav-links">
        <li><a href="#">Home</a></li>
        <li>
          <a href="#" class="has-drop">About</a>
          <div class="dropdown">
            <a href="#">Our Story</a>
            <a href="#">Our Philosophy</a>
            <a href="#">Meet the Team</a>
          </div>
        </li>
        <li>
          <a href="#" class="has-drop">Programs</a>
          <div class="dropdown">
            <a href="#">1:1 Coaching</a>
            <a href="#">Group Circles</a>
            <a href="#">Online Courses</a>
          </div>
        </li>
        <li>
          <a href="#" class="has-drop">Services</a>
          <div class="dropdown">
            <a href="#">Energy Healing</a>
            <a href="#">Breathwork</a>
            <a href="#">Sound Therapy</a>
            <a href="#">Teen Wellbeing</a>
          </div>
        </li>
        <li><a href="#">Retreats</a></li>
        <li><a href="#">Events</a></li>
        <li><a href="#">Blog</a></li>
        <li><a href="#">Contact</a></li>
        <li><a href="#" class="nav-cta">Book Now</a></li>
      </ul>
    </nav>

    <!-- HERO -->
    <section class="hero">
      <div class="hero-pattern"></div>
      <div class="hero-content">
        <div class="hero-eyebrow">Wholeness · Presence · Transformation</div>
        <h1>You are already<br /><em>whole</em></h1>
        <p>At i am ONE with, we help you remember what you've always known — that true wellness is not something you find, but something you return to.</p>
        <div class="hero-buttons">
          <a href="#" class="btn btn-light">Explore Our Offerings</a>
          <a href="#" class="btn btn-outline">Book a Session</a>
        </div>
      </div>
      <div class="hero-scroll">Scroll</div>
    </section>

    <!-- INTRO STRIP -->
    <section class="intro-strip">
      <p>
        We are all connected — to ourselves, to each other, to the earth.<br />
        <span>i am ONE with</span> is the space where that truth becomes lived experience.
      </p>
    </section>

    <!-- SERVICES -->
    <section class="services">
      <div class="section-header">
        <div class="eyebrow">What We Offer</div>
        <h2>Pathways to <em>wholeness</em></h2>
        <p>Every offering is an invitation to come home to yourself. Choose the path that calls to you.</p>
      </div>
      <div class="services-grid">

        <div class="service-card">
          <div class="service-img" style="background:linear-gradient(135deg,#e8f6f5,#c5e8e5);">🌿</div>
          <div class="service-body">
            <div class="service-tag">1:1 Journey</div>
            <h3>Integrative Health Coaching</h3>
            <p>A deeply personalised journey exploring the root causes of imbalance across mind, body, and spirit. Together we map your path back to vitality and inner alignment.</p>
            <a href="#" class="service-link">Learn More</a>
          </div>
        </div>

        <div class="service-card">
          <div class="service-img" style="background:linear-gradient(135deg,#edf3f8,#c5d8e8);">✨</div>
          <div class="service-body">
            <div class="service-tag">Energy Work</div>
            <h3>Intuitive Reiki</h3>
            <p>Release what no longer serves you. Through intuitive Reiki energy work, we restore the natural flow of life force through your whole being.</p>
            <a href="#" class="service-link">Learn More</a>
          </div>
        </div>

        <div class="service-card">
          <div class="service-img" style="background:linear-gradient(135deg,#fdf6e8,#f0dfa0);">🌬️</div>
          <div class="service-body">
            <div class="service-tag">Breath & Body</div>
            <h3>Breathwork Journeys</h3>
            <p>Conscious connected breathing that bypasses the thinking mind and accesses the body's innate intelligence. Profound, transformative, and always safe.</p>
            <a href="#" class="service-link">Learn More</a>
          </div>
        </div>

        <div class="service-card">
          <div class="service-img" style="background:linear-gradient(135deg,#faeef4,#e8b8cc);">🫂</div>
          <div class="service-body">
            <div class="service-tag">Community</div>
            <h3>Teen Wellbeing Program</h3>
            <p>Empowering young people with the tools of self-awareness, emotional intelligence, and nervous system regulation — skills for life.</p>
            <a href="#" class="service-link">Learn More</a>
          </div>
        </div>

      </div>
    </section>

    <!-- SPLIT FEATURE -->
    <section class="feature-split">
      <div class="feature-visual">
        <span class="feature-visual-icon">🧘</span>
      </div>
      <div class="feature-text">
        <div class="eyebrow">Our Approach</div>
        <h2>Healing that honours the <em>whole</em> of you</h2>
        <p>We don't believe in quick fixes or one-size-fits-all solutions. We believe in you — your wisdom, your capacity, your timing. Our role is simply to walk beside you.</p>
        <ul class="feature-list">
          <li>Trauma-informed and evidence-based practice</li>
          <li>Honouring body, mind, emotion & spirit equally</li>
          <li>Indigenous and ancestral wisdom integrated with care</li>
          <li>Safe, grounded containers for deep work</li>
          <li>Long-term wellbeing, not just symptom relief</li>
        </ul>
        <a href="#" class="btn" style="background:var(--navy);color:#fff;align-self:flex-start;margin-top:0.5rem;">About Our Philosophy</a>
      </div>
    </section>

    <!-- EVENTS -->
    <section class="events">
      <div class="section-header">
        <div class="eyebrow">Upcoming</div>
        <h2>Events & <em>Gatherings</em></h2>
        <p>Come as you are. Leave changed. Our events are held with care, intention, and space for every part of you.</p>
      </div>
      <div class="events-coming-soon">
        <div class="coming-soon-icon">🌙</div>
        <h3>Something beautiful is coming</h3>
        <p>Events and gatherings are being planned. Be the first to know when they're announced.</p>
        <a href="#" class="btn" style="background:var(--navy);color:#fff;margin-top:0.5rem;">Join the waitlist</a>
      </div>
    </section>

    <!-- TESTIMONIALS -->
    <section class="testimonials">
      <div class="section-header">
        <div class="eyebrow">Stories of Return</div>
        <h2>What our community says</h2>
        <p>These words belong to the brave humans who chose themselves.</p>
      </div>
      <div class="testimonials-track-wrap">
        <div class="testimonials-track">
          <div class="testimonial-slide active">
            <p class="testimonial-quote">I came in feeling completely lost and disconnected. Twelve weeks later I feel more myself than I have in years. This work is the real thing.</p>
            <div class="testimonial-author">
              <strong>Sarah M.</strong>
              1:1 Coaching Client — Sydney
            </div>
          </div>
          <div class="testimonial-slide">
            <p class="testimonial-quote">The breathwork session cracked me open in the most beautiful way. I released grief I didn't even know I was carrying. Absolutely life-changing.</p>
            <div class="testimonial-author">
              <strong>James T.</strong>
              Breathwork Journey Participant
            </div>
          </div>
          <div class="testimonial-slide">
            <p class="testimonial-quote">My daughter has completely transformed through the Teen Wellbeing Program. She has tools now that I wish I'd had at her age. We are so grateful.</p>
            <div class="testimonial-author">
              <strong>Michelle K.</strong>
              Parent — Teen Wellbeing Program
            </div>
          </div>
          <div class="testimonial-slide">
            <p class="testimonial-quote">I was sceptical about energy healing. Now it's a regular part of my life. The shift in my body and mind is undeniable. I feel lighter, clearer, more alive.</p>
            <div class="testimonial-author">
              <strong>David R.</strong>
              Energy Healing Client
            </div>
          </div>
        </div>
        <div class="testimonial-dots">
          <button class="dot active" data-idx="0" aria-label="Testimonial 1"></button>
          <button class="dot" data-idx="1" aria-label="Testimonial 2"></button>
          <button class="dot" data-idx="2" aria-label="Testimonial 3"></button>
          <button class="dot" data-idx="3" aria-label="Testimonial 4"></button>
        </div>
      </div>
    </section>

    <!-- PHILOSOPHY STRIP -->
    <section class="philosophy">
      <h2>"The wound is the place where the Light enters you."<br /><small style="font-size:0.55em;font-style:normal;opacity:0.85;">— Rumi</small></h2>
      <a href="#" class="btn">Begin Your Journey</a>
    </section>

    <!-- FOOTER -->
    <footer>
      <div class="footer-grid">
        <div class="footer-brand">
          <img src="/logo.png" alt="i am ONE with" />
          <p>A space for wholeness, healing, and remembering who you truly are. Serving Sydney and beyond.</p>
        </div>
        <div class="footer-col">
          <h4>Explore</h4>
          <ul>
            <li><a href="#">About Us</a></li>
            <li><a href="#">Services</a></li>
            <li><a href="#">Programs</a></li>
            <li><a href="#">Retreats</a></li>
            <li><a href="#">Events</a></li>
          </ul>
        </div>
        <div class="footer-col">
          <h4>Support</h4>
          <ul>
            <li><a href="#">Contact</a></li>
            <li><a href="#">FAQs</a></li>
            <li><a href="#">Gift Cards</a></li>
            <li><a href="#">Blog</a></li>
          </ul>
        </div>
        <div class="footer-col">
          <h4>Legal</h4>
          <ul>
            <li><a href="#">Privacy Policy</a></li>
            <li><a href="#">Terms of Service</a></li>
            <li><a href="#">Cancellation Policy</a></li>
          </ul>
        </div>
      </div>
      <div class="footer-bottom">
        <div>© 2026 i am ONE with. All rights reserved.</div>
        <div class="social-links">
          <a href="#" aria-label="Facebook">f</a>
          <a href="#" aria-label="Instagram">ig</a>
          <a href="#" aria-label="Email">✉</a>
        </div>
      </div>
    </footer>

    <script>
      // Testimonial carousel
      const slides = document.querySelectorAll('.testimonial-slide');
      const dots   = document.querySelectorAll('.dot');
      let current  = 0;
      let timer;

      function goTo(idx) {
        slides[current].classList.remove('active');
        dots[current].classList.remove('active');
        current = idx;
        slides[current].classList.add('active');
        dots[current].classList.add('active');
      }

      function next() { goTo((current + 1) % slides.length); }

      dots.forEach(dot => {
        dot.addEventListener('click', () => {
          clearInterval(timer);
          goTo(+dot.dataset.idx);
          timer = setInterval(next, 5000);
        });
      });

      timer = setInterval(next, 5000);
    </script>

  </body>
</html>
